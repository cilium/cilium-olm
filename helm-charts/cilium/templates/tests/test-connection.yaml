apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cilium.fullname" . }}-test-connection"
  labels:
    {{- include "cilium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cilium.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
